## 📋 批量分类完整执行流程

```
┌─────────────────────────────────────────────────────────────────┐
│                    第1步：点击 "5. 文献分类"                       │
│  位置：scripts/page1_gui.py 第 164 行                           │
│  按钮：ttk.Button("5. 文献分类", command=self._action_classify)  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    第2步：弹出选择对话框                           │
│  位置：scripts/page1_gui.py 第 710-718 行                       │
│  选择："3. AI 批量分类（推荐！快 4 倍，20 篇/批）"              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    第3步：点击 "开始分类"                          │
│  位置：scripts/page1_gui.py 第 918 行                           │
│  触发：do_classify() → method == "batch" 分支                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  第4步：在后台线程执行 run_batch()                               │
│  位置：scripts/page1_gui.py 第 879-916 行                        │
│                                                                  │
│  def run_batch():                                                │
│      stats = workflow.classify_papers_ai_batch(                 │
│          progress_callback=progress_callback,                     │
│          batch_size=50  # ✅ 已修改为 50                         │
│      )                                                            │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  第5步：Page1Workflow.classify_papers_ai_batch()              │
│  位置：src/workflow/page1_workflow.py 第 997-1059 行            │
│                                                                  │
│  1. 加载 metadata.json → papers[686篇]                         │
│  2. 创建 BatchPaperClassifier                                   │
│  3. 调用 classifier.classify_papers_batch()                     │
│  4. 保存更新后的 papers 到 metadata.json                        │
│  5. 调用 _create_category_directories() 创建分类目录             │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  第6步：BatchPaperClassifier.classify_papers_batch()          │
│  位置：src/workflow/batch_classify.py 第 71-203 行              │
│                                                                  │
│  1. 找出未分类的文献 → unclassified_papers[686篇]               │
│  2. 分批处理循环：                                               │
│     ├─ 批次 1: 文献 1-50    → 构造 Prompt → 调用 GLM API          │
│     ├─ 批次 2: 文献 51-100  → 构造 Prompt → 调用 GLM API          │
│     ├─ 批次 3: 文献 101-150 → 构造 Prompt → 调用 GLM API          │
│     └─ ... 共 14 批 (686 ÷ 50 = 13.72，向上取整 = 14 批)        │
│                                                                  │
│  3. 每批的处理流程：                                             │
│     a) 构造 system_prompt（领域定义）                            │
│     b) 构造 user_message（50 篇文献的标题/摘要/关键词）          │
│     c) 调用 api_client.generate()                                 │
│     d) 解析 JSON 响应                                            │
│     e) 更新 papers 的 classification 字段                          │
│     f) 调用 progress_callback(current, total, message)           │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  第7步：_create_category_directories()                          │
│  位置：src/workflow/page1_workflow.py 第 1180-1220 行            │
│                                                                  │
│  1. 按 primary_domain_cn 分组                                    │
│  2. 为每个领域创建目录：categories/气动优化/                     │
│  3. 复制 PDF 到对应目录                                         │
│  4. 生成 题录.json                                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  第8步：返回统计信息并显示                                       │
│  位置：scripts/page1_gui.py 第 886-906 行                        │
│                                                                  │
│  弹出对话框显示：                                                 │
│    - 总文献数: 686                                              │
│    - 本次分类: 686                                              │
│    - 领域分布:                                                   │
│      气动优化: 100 篇                                           │
│      风机控制: 80 篇                                             │
│      ...                                                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## ⏱️ 时间估算（修改后）

- **每批文献数**: 50 篇（已修改）
- **总批次数**: 686 ÷ 50 = 13.72 → **14 批**
- **每批耗时**: 约 30 秒
- **总耗时**: 14 × 30 秒 = **7 分钟**

---

## 📝 关键代码位置

| 步骤 | 文件 | 行号 | 说明 |
|------|------|------|------|
| GUI 入口 | `scripts/page1_gui.py` | 710-918 | `_action_classify()` 方法 |
| 批量调用 | `scripts/page1_gui.py` | 879-884 | `run_batch()` 调用 workflow |
| Workflow 入口 | `src/workflow/page1_workflow.py` | 997-1059 | `classify_papers_ai_batch()` 方法 |
| 核心分类器 | `src/workflow/batch_classify.py` | 71-203 | `BatchPaperClassifier.classify_papers_batch()` |
| 进度回调 | `scripts/page1_gui.py` | 871-877 | `progress_callback()` 函数 |

---

哼，现在笨蛋应该清楚了吧？重新启动 GUI 再试试！o(￣▽￣)ｄ
